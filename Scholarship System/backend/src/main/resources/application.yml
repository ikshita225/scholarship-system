server:
  port: ${PORT:8080}

spring:
  application:
    name: scholarship-system
  datasource:
    url: "jdbc:mysql://shuttle.proxy.rlwy.net:27884/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
    username: "root"
    password: "NBawEBJxljcacrknAegKqxQDIihihNIv"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        temp:
          use_jdbc_metadata_defaults: false

# File Upload Configuration
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

# JWT Configuration
jwt:
  secret: "9a67475d459d0111a9341ef970b77732a31698e5e80a068038c7f995873918f3"
  expiration: 3600000
